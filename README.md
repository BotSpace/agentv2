# Botmother Flow Agent

Python + LangGraph asosidagi CLI agent. U Botmother Engine loyihasi uchun flow DSL yozadi, mavjud flow'larni import qiladi, runtime JSON'ga kompilyatsiya qiladi va Ollama orqali interaktiv chat rejimida ishlaydi.

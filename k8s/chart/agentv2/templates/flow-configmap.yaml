apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentv2.name" . }}-flow-seed
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "agentv2.labels" . | nindent 4 }}
data:
  flow.json: |
{{ .Values.flow.seedJson | indent 4 }}
